---
description: Show SME bank dashboard as HTML — balances and recent transactions
argument-hint: "[account hint or 'all']"
---

Show my bank dashboard for: $ARGUMENTS

Use the `bank-dashboard` skill. Fetch accounts/transactions and write the HTML dashboard from the skill template. Read-only.
