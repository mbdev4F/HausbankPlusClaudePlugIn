---
description: Where is my payment — SWIFT network / UETR and SEPA Instant status (correlation / E2E id)
argument-hint: "[UETR | correlationId | paymentId]"
---

Find payment status / whereabouts: $ARGUMENTS

Use the `payment-search` skill (and `payment-search-agent` when available).
