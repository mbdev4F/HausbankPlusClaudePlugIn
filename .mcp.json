{
  "mcpServers": {
    "HausbankAgent": {
      "type": "http",
      "url": "${HAUSBANK_AGENT_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${HAUSBANK_AGENT_MCP_TOKEN}"
      }
    }
  }
}
