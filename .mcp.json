{
  "mcpServers": {
    "db-cb-connect": {
      "type": "http",
      "url": "${CB_CONNECT_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${CB_CONNECT_MCP_TOKEN}"
      }
    }
  }
}
